import {StyleSheet} from 'react-native'

import {ICON_IMAGE_SIDE} from '../../../constant'
const style = {
  topRightIcon: {
    marginRight: 20
  },
  iconImg: {
    width: ICON_IMAGE_SIDE,
    height: ICON_IMAGE_SIDE
  }
}

export default StyleSheet.create(style)
